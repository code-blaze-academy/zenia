from django.urls import path
from zenia_app_root.geometries_management import views

urlpatterns = [
    # path('render/', views.CubeView.as_view(), name='cube_render'),
    # path('render_cone/', views.ConeView.as_view(), name='render_cone'),
    
]